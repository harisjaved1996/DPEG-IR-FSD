import { LightningElement } from "lwc";

export default class PaymentActionCom extends LightningElement {
  /**
   * Handles the "Sync all accounts" action.
   * No-op stub for now; wiring only.
   */
  handleSyncAllAccounts() {
    // TODO: implement sync all accounts behavior
  }

  /**
   * Handles the "Link bank via Plaid" action.
   * No-op stub for now; wiring only.
   */
  handleLinkBankViaPlaid() {
    // TODO: implement link bank via Plaid behavior
  }
}
